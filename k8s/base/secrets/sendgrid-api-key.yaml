# Workshop placeholder — replace before deploy (never commit real keys).
apiVersion: v1
kind: Secret
metadata:
  name: sendgrid-api-key
  namespace: production
  labels:
    app.kubernetes.io/part-of: shopwave
type: Opaque
stringData:
  SENDGRID_KEY: "SG.workshop-placeholder-replace-me"
